{
  "name": "web-pkp",
  "version": "0.1.0",
  "private": true,
  "description": "Company Knowledge Center",
  "type": "module",
  "scripts": {
    "dev": "next dev",
    "build": "next build && pagefind --site out --output-subdir _pagefind",
    "postbuild": "echo 'Static site exported to ./out'",
    "start": "next start",
    "serve": "npx serve out"
  },
  "dependencies": {
    "next": "^15.1.0",
    "nextra": "^4.0.0",
    "nextra-theme-docs": "^4.0.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "pagefind": "^1.3.0"
  },
  "overrides": {
    "zod": "4.1.12"
  }
}
